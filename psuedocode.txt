Commands
    att (attnum) (val) : Sets attenutator num attnum to val
    reset att1/att2/att3/synth/pa/prbs/switch/transmitter
    synth (regnum) (val): Sets synth mem[regnum] to int val
    pa 1/0: sets PA chain to on or off
    get PRBS: retusn PRBS next number
    switch 1/0: turn mod switch on or off
    transmit (int data): transmits data 
    revert: reverts to old state, only does anything if you had just switcehd to hardware control, changed things, and we now wan tto go back to hw control


while True:
	state = 'sw_control'
    state_valid = True

	while (state == sw_control)
        if state_valid == False:
            pritn("prob should revert unless u def didnt touch anything in sw mode!!!")
        while (character not newline)
            if gpio == sw:
                state_valid=False
                sleep(a bit to debounce)
                state==sw_control
                break
            get new chars
        if state==sw break
        process command, do the thing


	while (state==hw_control)	
		if gpio==hw
			sleep(a bit)
			satte=hw_control
        




    // int state = 0;
    // int i=0;
    // for (int i=0; i < 10; i++) {
    //     printf("Hello, world!\n");
    //     gpio_put(gpios[i], 1);
    //     sleep_ms(1000);
    //     state = not state;
    //     gpio_put(gpios[i], 0);
    // }
    //mainloop(false, transmitter);